package baseline;

public class showCompleted extends ApplicationsController {
    int counter = 0;
    public int showComplete(){
        //creates an array list and assigns it to getTodoList
        //looks through every index value of the arrayList
            //parse through the information
                //if the completed number  is equal to 1 then add it to the display
                //show all the ones that are completed in the gui
                //increase counter
        return counter;
    }
}
