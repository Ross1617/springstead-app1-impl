package baseline;

import java.util.ArrayList;

public class editDescription extends ApplicationsController{
    ArrayList<String> newTodoList = new ArrayList<>();
    public ArrayList<String> editDescription(String newDescription, int index){
        //calls in getTodoList and assigns it to newTodoList
        //goes to the correct index number
        //parses through the string and saves the date and completed
        //takes the newDescription information and date and completed and concatinates and replaces the value
        //updates the table in the gui
        return newTodoList;
    }
}
