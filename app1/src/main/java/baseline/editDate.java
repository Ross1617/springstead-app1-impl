package baseline;

import java.util.ArrayList;

public class editDate extends ApplicationsController {
    ArrayList<String> newTodoList = new ArrayList<>();
    public ArrayList<String> editDate(String newDate ,int index){
        //calls in getTodoList and assigns it to newTodoList
        //goes to the correct index number
        //parses through the string and saves description and completed
        //takes the newDate information and concatinates with description anc completed
        //updates the table in the gui
        //update the newarraylist
        return newTodoList;
    }

}
