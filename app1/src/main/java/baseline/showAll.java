package baseline;

public class showAll extends ApplicationsController {
    int counter = 0;
    public int showAll(){
        //creates an array list and assigns it to getTodoList
            //goes through every index of the superclasses arrayLists
            //displays all of them in the
            //increase counter
        return counter;
    }
}
