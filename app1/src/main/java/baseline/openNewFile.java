package baseline;

public class openNewFile extends ApplicationsController {
    public void openNewFile(String fileName) {
        //saves and closes the current file
        //goes into the directory and looks for the new file
        //opens the new file
    }
}
