package baseline;

import java.util.ArrayList;
//this is the class that marks if something is complete or not
public class editCompleted extends ApplicationsController {
    ArrayList<String> newTodoList = new ArrayList<>();
    public ArrayList<String> editCompleted(int completed, int index){
        //calls in getTodoList and assigns it to newTodoList
        //goes to the correct index number
        //parses through the string and saves description and date
        //takes the completed information and replaces it with description and date
        //updates the table in the gui
        //updates the new Array List
        return newTodoList;
    }

}
